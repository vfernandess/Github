package com.voidx.search.navigation

import androidx.navigation.NavController

class SearchNavigatorImpl(private val navigation: NavController): SearchNavigator {

    override fun showRepoSearch() {
    }
}